// import { IPaymentInitialize, IPaymentInitializeResponse, IPaymentVerifyResponse } from "../types";
// //import { verifyPayment, initializePayment } from "../config/paystack";

// class PaymentService{
//     async initialize(data: IPaymentInitialize): Promise<IPaymentInitializeResponse>{
//         const response = await initializePayment(data);
//         return response;
//     }

//     async verify(reference:string): Promise<IPaymentVerifyResponse>{
//         const response = await verifyPayment(reference);
//         return response;
//     }

// }

// export default new PaymentService();